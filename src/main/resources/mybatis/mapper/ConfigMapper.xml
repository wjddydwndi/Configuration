<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.configuration.mapper.ConfigMapper">

    <select id="selectConfiguration" resultType=""
    <select id="selectUpdateList" resultType="Configuration">
        SELECT 't_configuration' AS tname , count(*) as cnt, max(update_time) AS updateTime from t_configuration
        /*UNION
        SELECT 't_configuration' AS tname , count(*) as cnt, max(update_time) AS updateTime from t_configuration*/

    </select>
</mapper>
